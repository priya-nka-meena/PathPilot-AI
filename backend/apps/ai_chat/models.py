from django.db import models

# Placeholder for AI chat related models (messages, conversations) — to be implemented later
class AIChatPlaceholder(models.Model):
    created = models.DateTimeField(auto_now_add=True)

    class Meta:
        abstract = True
